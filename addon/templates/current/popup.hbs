{{#if hasBlock}}{{setHasBlock}}{{/if}}

{{#if (and popupOpen popupLocation popupOffset)}}
  {{#popup-layer
    location=popupLocation
    offset=popupOffset
    closePopup=(action setPopupOpen false)}}
      {{yield}}
  {{/popup-layer}}
{{/if}}
